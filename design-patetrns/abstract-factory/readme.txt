requirements
1. client can create a GUI of any type during runtime. ex: mac, window
2. in the GUI, client can create any type of button at runtime which behaves differently on clicking
    (ex: pop-out, blink etc...)
3. It should be easy to add new type of GUI and buttons with minimal code changes